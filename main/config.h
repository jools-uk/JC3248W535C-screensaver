#pragma once

/* ── Project configuration ────────────────────────────────────────────────── */

/* Display rotation.  display.h defaults to 0 (portrait) if not set here.
 *   0   = portrait          (no rotation)
 *   90  = landscape CW      (portrait right becomes top)
 *   180 = portrait flipped
 *   270 = landscape CCW     (portrait left becomes top)  */
#define DISP_ROTATE  90
